#include <bits/stdc++.h>
using namespace std;
int main(){
    int n=17;
    int last=min(n,100);
    for(int i=1;i<=last;i++){
        if (n%i==0){
            cout<<i<<endl;
        }
    }
    return 0;
}
